import type { Metadata } from "next/types"

export const metadata: Metadata = {
    title: "Blog",
    description: "...Blog descr",
  }
const Blog = () => {

    return <div> blog</div>
}

export default Blog